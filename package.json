{
  "name": "chatnest",
  "version": "1.0.0",
  "description": "A real-time chat application for language learners",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run preview",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
